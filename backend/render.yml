services:
  - type: web
    name: b-tranz-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9